<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gps">

	<insert id="insertGps" parameterType="cardiacVo">
		insert into gps values(GPS_ID.nextval, #{mem_id}, #{gold_st},to_date(sysdate,'yy/mm/dd hh24:mi',#{gps_lo},#{gps_la} #{car_bpm}))
	</insert>

</mapper>